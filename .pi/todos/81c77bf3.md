{
  "id": "81c77bf3",
  "title": "Add diff code block support",
  "tags": [
    "parser",
    "diff"
  ],
  "status": "done",
  "created_at": "2026-08-08T04:44:26.815Z"
}

Add a markdown preprocessor (like Multicodeblock) that turns `{.lang .diff}` fenced code blocks into highlighted code with added/removed line coloring. Uses pandoc fenced-code-attributes (standard) + generated `<style>` scoped to pandoc's per-line span ids. Plus CSS vars + update example post + tests.
