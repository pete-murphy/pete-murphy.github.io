<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>WIP</title>
  <link href="https://pete-murphy.github.io" rel="self" type="application/rss+xml" />
  <updated></updated>
  <author>
    <name>Pete</name>
  </author>
  <id>https://pete-murphy.github.io/</id>

  <entry>
    <title>Notes on free monads</title>
    <link href="https://pete-murphy.github.ioposts/2026-07-31-free-monad-in-plain-english.html"/>
    <id>https://pete-murphy.github.ioposts/2026-07-31-free-monad-in-plain-english.html</id>
    <updated>2026-07-31T00:00:00Z</updated>
    <category term="haskell"/>
    <category term="free"/>
    <category term="monad"/>
    <summary></summary>
    <content type="html"><![CDATA[<p>Free monads are things that I’ve heard about, and feel like I mostly
get, but I haven’t had an opportunity to use them <em>in anger</em> to
really cement the intuition. So I’m writing down a bunch of related
thoughts and intuitions that have helped, to refer back to in lieu of
experience.</p>
<h2 id="a-tree-with-f-shaped-branches">A tree with <code>f</code>-shaped
branches</h2>
<p><a
href="https://www.reddit.com/r/haskell/comments/2znhjk/comment/cplwj3e/">Kmett
says</a>, “you can think of <code>Free f a</code> as a tree with an
<code>f</code>-shaped branching structure.” <a
href="https://www.youtube.com/watch?v=eKkxmVFcd74">Nate Faubion’s “Free
from tree” presentation</a> elaborates on this: plug in
<code>Pair</code> for <code>f</code> and you’ve got a binary search tree
(though more practically, you’d want <code>Compose Pair Maybe</code> to
allow for trees with more than just an even number of branches at each
node).</p>
<div class="sourceCode" id="cb1"><pre
class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true" tabindex="-1"></a><span class="kw">data</span> <span class="dt">Pair</span> a <span class="ot">=</span> <span class="dt">Pair</span> a a</span>
<span id="cb1-2"><a href="#cb1-2" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb1-3"><a href="#cb1-3" aria-hidden="true" tabindex="-1"></a><span class="kw">data</span> <span class="dt">Free</span> f a</span>
<span id="cb1-4"><a href="#cb1-4" aria-hidden="true" tabindex="-1"></a>  <span class="ot">=</span> <span class="dt">Pure</span> a</span>
<span id="cb1-5"><a href="#cb1-5" aria-hidden="true" tabindex="-1"></a>  <span class="op">|</span> <span class="dt">Roll</span> (f (<span class="dt">Free</span> f a))</span>
<span id="cb1-6"><a href="#cb1-6" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb1-7"><a href="#cb1-7" aria-hidden="true" tabindex="-1"></a><span class="kw">type</span> <span class="dt">BST</span> a <span class="ot">=</span> <span class="dt">Free</span> <span class="dt">Pair</span> a</span>
<span id="cb1-8"><a href="#cb1-8" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb1-9"><a href="#cb1-9" aria-hidden="true" tabindex="-1"></a><span class="ot">tree ::</span> <span class="dt">BST</span> <span class="dt">Int</span></span>
<span id="cb1-10"><a href="#cb1-10" aria-hidden="true" tabindex="-1"></a>tree <span class="ot">=</span></span>
<span id="cb1-11"><a href="#cb1-11" aria-hidden="true" tabindex="-1"></a>  <span class="dt">Roll</span> (<span class="dt">Pair</span></span>
<span id="cb1-12"><a href="#cb1-12" aria-hidden="true" tabindex="-1"></a>    (<span class="dt">Roll</span> (<span class="dt">Pair</span></span>
<span id="cb1-13"><a href="#cb1-13" aria-hidden="true" tabindex="-1"></a>      (<span class="dt">Pure</span> <span class="dv">1</span>)</span>
<span id="cb1-14"><a href="#cb1-14" aria-hidden="true" tabindex="-1"></a>      (<span class="dt">Pure</span> <span class="dv">3</span>)))</span>
<span id="cb1-15"><a href="#cb1-15" aria-hidden="true" tabindex="-1"></a>    (<span class="dt">Pure</span> <span class="dv">7</span>))</span></code></pre></div>
<h2 id="static-analysis">Static analysis</h2>
<p>One of the perceived benefits of expressing your program in terms of
<code>Free</code> is that you might be able to statically analyze it (as
a pure data structure), or do a “dry run”. This is true for an AST built
out of operators like</p>
<div class="sourceCode" id="cb2"><pre
class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb2-1"><a href="#cb2-1" aria-hidden="true" tabindex="-1"></a><span class="kw">data</span> <span class="dt">Operator</span> a</span>
<span id="cb2-2"><a href="#cb2-2" aria-hidden="true" tabindex="-1"></a>  <span class="ot">=</span> <span class="dt">Add</span> a a</span>
<span id="cb2-3"><a href="#cb2-3" aria-hidden="true" tabindex="-1"></a>  <span class="op">|</span> <span class="dt">Sub</span> a a</span>
<span id="cb2-4"><a href="#cb2-4" aria-hidden="true" tabindex="-1"></a>  <span class="op">|</span> <span class="dt">Mul</span> a a</span>
<span id="cb2-5"><a href="#cb2-5" aria-hidden="true" tabindex="-1"></a>  <span class="op">|</span> <span class="dt">Neg</span> a</span></code></pre></div>
<p>But, it doesn’t seem particularly useful to wrap
<code>Operation</code> in <code>Free</code>? <code>do</code> notation
doesn’t add anything.</p>
<p>In the case where <code>f</code> in your <code>Free f</code> contains
a continuation (<code>f</code> has any data constructors that take a
lambda), for example</p>
<div class="sourceCode" id="cb3"><pre
class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb3-1"><a href="#cb3-1" aria-hidden="true" tabindex="-1"></a><span class="kw">data</span> <span class="dt">CmdF</span> next</span>
<span id="cb3-2"><a href="#cb3-2" aria-hidden="true" tabindex="-1"></a>  <span class="ot">=</span> <span class="dt">PutLine</span> <span class="dt">String</span> next</span>
<span id="cb3-3"><a href="#cb3-3" aria-hidden="true" tabindex="-1"></a>  <span class="op">|</span> <span class="dt">GetLine</span> (<span class="dt">String</span> <span class="ot">-&gt;</span> next)</span></code></pre></div>
<p>the program would not be introspectable, and you would need to
provide all your inputs up front to do a dry run. Probably obvious, but
something Kovanikov notes in his talk as a “weird thing” that gets
overlooked in tutorials.</p>
<h2 id="monad-is-to-monoid-as-foldfree-is-to-foldmap"><code>Monad</code>
is to <code>Monoid</code> as <code>foldFree</code> is to
<code>foldMap</code></h2>
<p><code>List</code> is called the “free monoid”, and
<code>foldFree</code> is kinda like <code>foldMap</code> if you squint
(and use squiggly arrow <code>~&gt;</code> for natural
transformations)</p>
<div class="sourceCode" id="cb4"><pre
class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb4-1"><a href="#cb4-1" aria-hidden="true" tabindex="-1"></a><span class="fu">foldMap</span></span>
<span id="cb4-2"><a href="#cb4-2" aria-hidden="true" tabindex="-1"></a><span class="ot">  ::</span> <span class="dt">Monoid</span> m</span>
<span id="cb4-3"><a href="#cb4-3" aria-hidden="true" tabindex="-1"></a>  <span class="ot">=&gt;</span> (a <span class="ot">-&gt;</span> m)</span>
<span id="cb4-4"><a href="#cb4-4" aria-hidden="true" tabindex="-1"></a>  <span class="ot">-&gt;</span> <span class="dt">FreeMonoid</span> a  <span class="co">-- aka [a]</span></span>
<span id="cb4-5"><a href="#cb4-5" aria-hidden="true" tabindex="-1"></a>  <span class="ot">-&gt;</span> m</span>
<span id="cb4-6"><a href="#cb4-6" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb4-7"><a href="#cb4-7" aria-hidden="true" tabindex="-1"></a>foldFree</span>
<span id="cb4-8"><a href="#cb4-8" aria-hidden="true" tabindex="-1"></a><span class="ot">  ::</span> <span class="dt">Monad</span> m</span>
<span id="cb4-9"><a href="#cb4-9" aria-hidden="true" tabindex="-1"></a>  <span class="ot">=&gt;</span> (f <span class="op">~&gt;</span> m)</span>
<span id="cb4-10"><a href="#cb4-10" aria-hidden="true" tabindex="-1"></a>  <span class="ot">-&gt;</span> <span class="dt">FreeMonad</span> f</span>
<span id="cb4-11"><a href="#cb4-11" aria-hidden="true" tabindex="-1"></a>  <span class="op">~&gt;</span> m</span></code></pre></div>
<p>Preserve your data (or AST) fully intact in the <code>Free*</code>
structure, and then plug in different <code>a -&gt; m</code> or
<code>f ~&gt; m</code> functions to get different interpretations of
it.</p>
<h2 id="performance">Performance</h2>
<p><a
href="https://youtu.be/3GKQ4ni2pS0?si=e-18CYU-C711Yu6K&amp;t=2371">There
is a claim that Church-encoded free monads are “fast”</a>. From what I
understand, the idea is that nested <code>&gt;&gt;=</code>’s are
quadratic when using the ordinary <code>Free</code> type, but can be
improved (to linear) when using Church encoding. There are maybe several
possible Church encodings? The canonical reference is Janis
Voigtländer’s <a
href="https://janis-voigtlaender.eu/papers/AsymptoticImprovementOfComputationsOverFreeMonads.pdf">“Asymptotic
Improvement of Computations over Free Monads”</a>, and then Kmett has
more to say in his <a
href="https://ekmett.github.io/reader/2011/free-monads-for-less/">“Free
monads for less” series</a>.</p>
<h2 id="freer">freer</h2>
<p><code>Free</code>’s <code>Monad</code> instance requires a
<code>Functor</code> constraint on <code>f</code>, <code>freer</code>
somehow bypasses this using <code>Coyoneda</code>. See <a
href="https://okmij.org/ftp/Haskell/extensible/more.pdf">“Freer Monads,
More Extensible Effects”</a>.</p>
<!-- Although `Free` is a tree (with `f`-shaped branches), in most cases you'll have a constructor for `f` that contains a lambda. -->
<!--


There's [a common pedagogical mistake](https://byorgey.wordpress.com/2009/01/12/abstraction-intuition-and-the-monad-tutorial-fallacy/) that teachers make when teaching about monads, which is to concentrate on some abstract intution that the teacher has reached, instead of the many concrete examples that helped them to reach that intuition. Something like mightybyte's [Monad Challenges](https://mightybyte.github.io/monad-challenges/pages/ex1-3.html) is a good example of the opposite approach: start with a few problems that you are likely to encounter in day-to-day programming (generating random numbers from a seed, handling sequences of failing computations, exploring all possible combinations of some set of values) and showing how they are similar and that the "monad" concept abstracts over all these use cases. The intuition is hardened by repeated exposure to similar examples, and

  -->
<p><!--


Pretty early on in my Haskell-learning, I heard about "free monads" and was mystified. I bounced off some Kmett blog posts, and when I asked for a simple explanation or some more beginner-friendly material to read up on (in the haskell-beginners Slack) I was told something like "it's a tree of `bind`s", but that didn't help my understanding at all. I continued to bounce off of different blog posts and explanations, playing out the [monad tutorial fallacy](), but with nothing really concrete to sink my teeth into (I have had plenty use cases for _plain_ monads, but no real uses for free monads after all).

I have struggled for a while with wrapping my head around what "free monad" means. I'm not even sure how to formulate that last sentence: is it "_the_ free monad"? or "free monads" (plural)? I have no idea, and will likely continue to mix those up through the rest of this piece.

I have read Kmett's lengthy Reddit post,

There's a understanding of "free monad" (or free structures in general) that I've latched onto. I should disclaim I don't really get the math, my intuition is rooted in Haskell, and so code examples and explanations will use Haskell.

## Free ... as in beer?

Why is it called a _"free"_ monad? Is it, "free as in beer"? "Free as in speech"? "Free as in \[some other thing\]"?

To answer this, I think it's helpful to look at some other "free" things. Haskellers are also known to say, "`List` is the free `Monoid`". Can we draw some generalizations from these two examples? `List` and `Free` are type constructors, `Monoid` and `Monad` are type classes. So I'm going to assert this: when a Haskeller says, **"`Foo` is the free `Bloop`"**, they are saying the following:

1. `Foo` is a _type constructor_ (meaning, it takes some other type as input, and returns a type). Specifically, `Foo` has kind `inkind → outkind` for some kinds `inkind` & `outkind`.
2. `Bloop` is a _type class_ for types of kind `outkind` (the kind that you get from partially-applying `Foo`)
3. You can write a `Bloop` instance for `Foo` applied to any input type of kind `inkind` (without knowing anything about that input type)

In **"`List` is the free `Monoid`"**. `List` is a type constructor, and I can write a `Monoid` instance for `List a` _for any type `a`,_ without knowing anything about `a`. In other words, the `Monoid` instance doesn't use anything specific to `a`, it just uses `List` operations.

```haskell
instance Monoid (List item) where
  (<>)   = (++)
  mempty = []
```

What are some other ones?

- `NonEmptyList` is the free `Semigroup` for similar reasons
- `Maybe` is the free `Default` because I can choose `def = Nothing` for any type
- `Compose Set List` is the free `Semiring`

So I think of this as "free as in beer": these are instances you get "for free" just by putting `a` into the structure of the type constructor, you don't need any further constraints on the `a`.

Note that "`Free` is the free `Monad`" is an outlier in a few ways:

- the `inkind` kind is `Type → Type` (the other examples have `inkind ∷ Type`)—i.e. it's a type constructor that takes a type constructor
- "free" is the name of the data type, but really it's not any _more_ free than the other types listed above—this seems like plain bad naming to me
- in fact, it's not even quite _as_ free as the others—it requires a `Functor` constraint on its input type (more on this below)

## Why though

This doesn't really satisfy the question of _why_ you might want to do this—what are you getting for free by using any of these free structures?

Here's a dumb but maybe illustrative example. Say you regularly play _Super Smash Bros_ (the N64 game) with your friends. You keep track of who's the best player by who has had the most KOs in a game. Maybe you write this down on a white board. As soon as someone scores higher than the current champion, you clear the board and write the new champion's name and score.

As more games are played, there's a tie for highest score. To break the tie, you'd like to change the criteria for best player: instead of all-time highest KOs, it should be whoever has won the most games total. Too bad! Nobody remembers how many games each player has won, all you've kept track of is KOs.

What you could have done instead, is kept a log of all the scores of each game (a `List Results`, where `Results` contains the game results: place/ranking and KOs per player). That way you can calculate the best player afterwards, using whatever criteria you'd like.

Calculating best player in different ways is like plugging in a different `Monoid` in a `foldMap` over the `List Result` log.

```haskell
foldMap
  :: Monoid m
  => (a -> m) -- this lets you pick the criteria
  -> [a]      -- your logs
  -> m        -- your result (best player)
```

If you use a function that turns `Results` into `(Max Int, First Player)` as your `(a -> m)` that's your max KOs, if you use `Results -> Map Player (Sum Int)` that would show you who has won the most games, etc.

There's a similar thing in `Free` called `foldFree`

```haskell
foldFree
  :: Monad m
  => (forall x. f x -> m x) -- this lets you pick how your AST is interpreted
  -> Free f a               -- your AST
  -> m a                    -- your result
```

Again, the naming kinda gets in the way: `foldFree` seems to be doing just as much "mapping" as `foldMap`, and as we saw both `Free` and `[]` are "free".

Aside: Hm, maybe this is "free as in speech" after all?

## Freer

Remember when we said that `Free` was an outlier because it required a `Functor` constraint on `f`? Turns out you can get `f`'s `Functor` instance "for free" (I think?) using something called `Coyoneda`. I don't yet understand what this is about, but apparently it's in this paper: https://okmij.org/ftp/Haskell/extensible/more.pdf
 --></p>]]></content>
  </entry>
  <entry>
    <title>Collecting successes and errors</title>
    <link href="https://pete-murphy.github.ioposts/2024-04-25-traversing-with-errors.html"/>
    <id>https://pete-murphy.github.ioposts/2024-04-25-traversing-with-errors.html</id>
    <updated>2024-04-25T00:00:00Z</updated>
    <category term="traverse"/>
    <category term="haskell"/>
    <category term="purescript"/>
    <category term="fp-ts"/>
    <category term="elm"/>
    <summary></summary>
    <content type="html"><![CDATA[<h2 id="traverse-map-with-effects"><code>traverse</code>
(“<code>map</code> with effects”)</h2>
<p>Often we want to iterate through a collection of items, performing
some effect for each item. This means we want some function that looks
like</p>
<div class="sourceCode" id="cb1"><pre
class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true" tabindex="-1"></a>(a <span class="ot">-&gt;</span> f b) <span class="ot">-&gt;</span> t a <span class="ot">-&gt;</span> result</span></code></pre></div>
<p>where <code>a -&gt; f b</code> is our effectful computation,
<code>t a</code> is our collection (typically a list or an array of
<code>a</code>s) and <code>result</code> could take a few different
shapes depending on the requirements of our program, especially in the
common case when the effect <code>f</code> encapsulates some notion of
failure (like <code>TaskEither</code> in <code>fp-ts</code>, or anything
with <code>ExceptT</code> in its stack in <code>Haskell</code>).</p>
<aside data-admonition="tip">
<p>The <strong>tl;dr</strong> is that you almost <em>always</em> want
(some version of) <code>traverse</code> in this situation—so often that
<a
href="https://impurepics.com/posts/2020-10-03-always-traverse.html">“the
answer is always <code>traverse</code>” has become a meme</a>.</p>
</aside>
<h2 id="a-decision-tree">A decision tree</h2>
<ul class="decision-tree">
<li>
<strong>Is there a notion of “partial” success (succeed with a
warning)?</strong>
<ul>
<li>
<strong>Yes:</strong> Use <code>traverse</code> with <code>These</code>.
</li>
<li>
<strong>No:</strong> Continue.
</li>
</ul>
</li>
<li>
<strong>If everything succeeds, do you want to collect <em>all</em>
successes?</strong>
<ul>
<li>
<strong>Yes:</strong>
<ul>
<li>
<strong>…and in case of failure I want to report just the first
failure.</strong>
<p>
Use <code>traverse</code> with <code>Either</code>/<code>ExceptT</code>.
</p>
</li>
<li>
<strong>…and in case of failure I want to report <em>all</em>
failures.</strong>
<p>
Use <code>traverse</code> with <code>Validation</code>.
</p>
</li>
</ul>
</li>
<li>
<strong>No:</strong> Continue.
</li>
</ul>
</li>
<li>
<strong>Do you want just the <em>first</em> success (if anything
succeeds)?</strong>
<ul>
<li>
<strong>Yes:</strong>
<ul>
<li>
<strong>…and in case of failure I want to report just the last
failure.</strong>
<p>
Use <code>altAll</code>/<code>asum</code>/
<code>choice</code>/<code>oneOf</code> with
<code>Either</code>/<code>ExceptT</code>.
</p>
</li>
<li>
<strong>…and in case of failure I want to report <em>all</em>
failures.</strong>
<p>
Use <code>altAll</code>/<code>asum</code>/
<code>choice</code>/<code>oneOf</code> with <code>Validation</code>.
</p>
</li>
</ul>
</li>
<li>
<strong>No:</strong> Continue.
</li>
</ul>
</li>
<li>
<strong>You want to just collect all successes and all
failures?</strong>
<p>
Use <code>wilt</code> (or <code>partitionMap</code> if the only “effect”
is error handling).
</p>
</li>
</ul>
<h2 id="section"></h2>
<p><code>wilt</code> is from the <code>Witherable</code> type class in
PureScript and <code>fp-ts</code>, and appears as <a
href="https://hackage.haskell.org/package/filtrable-0.1.6.0/docs/Data-Filtrable.html#v:mapEitherA"><code>mapEitherA</code></a>
in Haskell. Conceptually, “<code>partitionMap</code> with effects.” It’s
a rarer bird than <code>traverse</code>.</p>]]></content>
  </entry>
  <entry>
    <title>A syntax-driven intuition for Cont</title>
    <link href="https://pete-murphy.github.ioposts/2023-05-30-cont-and-managed.html"/>
    <id>https://pete-murphy.github.ioposts/2023-05-30-cont-and-managed.html</id>
    <updated>2023-05-30T00:00:00Z</updated>
    <category term="haskell"/>
    <category term="purescript"/>
    <category term="cont"/>
    <summary></summary>
    <content type="html"><![CDATA[<p>Based on the <a
href="https://hackage.haskell.org/package/managed-1.0.10/docs/src/Control.Monad.Managed.html#Managed">internal
accessor <code>(&gt;&gt;-)</code></a> from Gabriella Gonzalez’s
<code>managed</code> library. <em>Edit:</em> I re-discovered that
Gabriella had <a
href="https://www.haskellforall.com/2014/04/how-continuation-monad-works.html">posted
about “renaming things” like this</a> to get an intuition for
<code>ContT</code> on her blog. She also has another relevant post <a
href="https://www.haskellforall.com/2012/12/the-continuation-monad.html">explaining
how to use <code>Cont</code> in depth</a>.</p>
<p>PureScript allows us to override the default <code>bind</code>
implementation and re-purpose the <code>do</code> syntax sugar (we could
do the same in Haskell with <code>RebindableSyntax</code> enabled).</p>
<multicodeblock-tabs>
<multicodeblock-tab role="heading" slot="tab">PureScript (with infix operator)</multicodeblock-tab>
<multicodeblock-panel role="region" slot="panel">
<div class="sourceCode" id="cb1"><pre
class="sourceCode purescript"><code class="sourceCode purescript"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true" tabindex="-1"></a><span class="kw">import</span> <span class="dt">Prelude</span></span>
<span id="cb1-2"><a href="#cb1-2" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb1-3"><a href="#cb1-3" aria-hidden="true" tabindex="-1"></a><span class="kw">newtype</span> <span class="dt">Cont</span> r a <span class="ot">=</span> <span class="dt">Cont</span> ((a <span class="ot">-&gt;</span> r) <span class="ot">-&gt;</span> r)</span>
<span id="cb1-4"><a href="#cb1-4" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb1-5"><a href="#cb1-5" aria-hidden="true" tabindex="-1"></a><span class="ot">runCont ::</span> <span class="kw">forall</span> r a<span class="op">.</span> <span class="dt">Cont</span> r a <span class="ot">-&gt;</span> (a <span class="ot">-&gt;</span> r) <span class="ot">-&gt;</span> r</span>
<span id="cb1-6"><a href="#cb1-6" aria-hidden="true" tabindex="-1"></a>runCont (<span class="dt">Cont</span> f) <span class="ot">=</span> f</span>
<span id="cb1-7"><a href="#cb1-7" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb1-8"><a href="#cb1-8" aria-hidden="true" tabindex="-1"></a><span class="kw">infixl</span> <span class="dv">1</span> runCont as <span class="op">&gt;&gt;-</span></span>
<span id="cb1-9"><a href="#cb1-9" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb1-10"><a href="#cb1-10" aria-hidden="true" tabindex="-1"></a><span class="kw">instance</span> <span class="dt">Functor</span> (<span class="dt">Cont</span> r) <span class="kw">where</span></span>
<span id="cb1-11"><a href="#cb1-11" aria-hidden="true" tabindex="-1"></a>  <span class="fu">map</span> f ma <span class="ot">=</span></span>
<span id="cb1-12"><a href="#cb1-12" aria-hidden="true" tabindex="-1"></a>    <span class="dt">Cont</span> \pure&#39; <span class="ot">-&gt;</span></span>
<span id="cb1-13"><a href="#cb1-13" aria-hidden="true" tabindex="-1"></a>      ma <span class="op">&gt;&gt;-</span> \a <span class="ot">-&gt;</span></span>
<span id="cb1-14"><a href="#cb1-14" aria-hidden="true" tabindex="-1"></a>        pure&#39; (f a)</span>
<span id="cb1-15"><a href="#cb1-15" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb1-16"><a href="#cb1-16" aria-hidden="true" tabindex="-1"></a><span class="kw">instance</span> <span class="dt">Apply</span> (<span class="dt">Cont</span> r) <span class="kw">where</span></span>
<span id="cb1-17"><a href="#cb1-17" aria-hidden="true" tabindex="-1"></a>  apply mab ma <span class="ot">=</span></span>
<span id="cb1-18"><a href="#cb1-18" aria-hidden="true" tabindex="-1"></a>    <span class="dt">Cont</span> \pure&#39; <span class="ot">-&gt;</span> <span class="kw">do</span></span>
<span id="cb1-19"><a href="#cb1-19" aria-hidden="true" tabindex="-1"></a>      mab <span class="op">&gt;&gt;-</span> \ab <span class="ot">-&gt;</span></span>
<span id="cb1-20"><a href="#cb1-20" aria-hidden="true" tabindex="-1"></a>        ma <span class="op">&gt;&gt;-</span> \a <span class="ot">-&gt;</span></span>
<span id="cb1-21"><a href="#cb1-21" aria-hidden="true" tabindex="-1"></a>          pure&#39; (ab a)</span>
<span id="cb1-22"><a href="#cb1-22" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb1-23"><a href="#cb1-23" aria-hidden="true" tabindex="-1"></a><span class="kw">instance</span> <span class="dt">Applicative</span> (<span class="dt">Cont</span> r) <span class="kw">where</span></span>
<span id="cb1-24"><a href="#cb1-24" aria-hidden="true" tabindex="-1"></a>  <span class="fu">pure</span> a <span class="ot">=</span></span>
<span id="cb1-25"><a href="#cb1-25" aria-hidden="true" tabindex="-1"></a>    <span class="dt">Cont</span> \pure&#39; <span class="ot">-&gt;</span></span>
<span id="cb1-26"><a href="#cb1-26" aria-hidden="true" tabindex="-1"></a>      pure&#39; a</span>
<span id="cb1-27"><a href="#cb1-27" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb1-28"><a href="#cb1-28" aria-hidden="true" tabindex="-1"></a><span class="kw">instance</span> <span class="dt">Bind</span> (<span class="dt">Cont</span> r) <span class="kw">where</span></span>
<span id="cb1-29"><a href="#cb1-29" aria-hidden="true" tabindex="-1"></a>  bind ma amb <span class="ot">=</span></span>
<span id="cb1-30"><a href="#cb1-30" aria-hidden="true" tabindex="-1"></a>    <span class="dt">Cont</span> \pure&#39; <span class="ot">-&gt;</span></span>
<span id="cb1-31"><a href="#cb1-31" aria-hidden="true" tabindex="-1"></a>      ma <span class="op">&gt;&gt;-</span> \a <span class="ot">-&gt;</span></span>
<span id="cb1-32"><a href="#cb1-32" aria-hidden="true" tabindex="-1"></a>        amb a <span class="op">&gt;&gt;-</span> \b <span class="ot">-&gt;</span></span>
<span id="cb1-33"><a href="#cb1-33" aria-hidden="true" tabindex="-1"></a>          pure&#39; b</span></code></pre></div>
</multicodeblock-panel>
<multicodeblock-tab role="heading" slot="tab">PureScript (with do)</multicodeblock-tab>
<multicodeblock-panel role="region" slot="panel">
<div class="sourceCode" id="cb2"><pre
class="sourceCode purescript"><code class="sourceCode purescript"><span id="cb2-1"><a href="#cb2-1" aria-hidden="true" tabindex="-1"></a><span class="kw">import</span> <span class="dt">Prelude</span> <span class="kw">hiding</span> (bind)</span>
<span id="cb2-2"><a href="#cb2-2" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb2-3"><a href="#cb2-3" aria-hidden="true" tabindex="-1"></a><span class="kw">newtype</span> <span class="dt">Cont</span> r a <span class="ot">=</span> <span class="dt">Cont</span> ((a <span class="ot">-&gt;</span> r) <span class="ot">-&gt;</span> r)</span>
<span id="cb2-4"><a href="#cb2-4" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb2-5"><a href="#cb2-5" aria-hidden="true" tabindex="-1"></a><span class="ot">bind ::</span> <span class="kw">forall</span> r a<span class="op">.</span> <span class="dt">Cont</span> r a <span class="ot">-&gt;</span> (a <span class="ot">-&gt;</span> r) <span class="ot">-&gt;</span> r</span>
<span id="cb2-6"><a href="#cb2-6" aria-hidden="true" tabindex="-1"></a>bind (<span class="dt">Cont</span> f) <span class="ot">=</span> f</span>
<span id="cb2-7"><a href="#cb2-7" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb2-8"><a href="#cb2-8" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb2-9"><a href="#cb2-9" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb2-10"><a href="#cb2-10" aria-hidden="true" tabindex="-1"></a><span class="kw">instance</span> <span class="dt">Functor</span> (<span class="dt">Cont</span> r) <span class="kw">where</span></span>
<span id="cb2-11"><a href="#cb2-11" aria-hidden="true" tabindex="-1"></a>  <span class="fu">map</span> f ma <span class="ot">=</span></span>
<span id="cb2-12"><a href="#cb2-12" aria-hidden="true" tabindex="-1"></a>    <span class="dt">Cont</span> \pure&#39; <span class="ot">-&gt;</span> <span class="kw">do</span></span>
<span id="cb2-13"><a href="#cb2-13" aria-hidden="true" tabindex="-1"></a>      a <span class="ot">&lt;-</span> ma</span>
<span id="cb2-14"><a href="#cb2-14" aria-hidden="true" tabindex="-1"></a>      pure&#39; (f a)</span>
<span id="cb2-15"><a href="#cb2-15" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb2-16"><a href="#cb2-16" aria-hidden="true" tabindex="-1"></a><span class="kw">instance</span> <span class="dt">Apply</span> (<span class="dt">Cont</span> r) <span class="kw">where</span></span>
<span id="cb2-17"><a href="#cb2-17" aria-hidden="true" tabindex="-1"></a>  apply mab ma <span class="ot">=</span></span>
<span id="cb2-18"><a href="#cb2-18" aria-hidden="true" tabindex="-1"></a>    <span class="dt">Cont</span> \pure&#39; <span class="ot">-&gt;</span> <span class="kw">do</span></span>
<span id="cb2-19"><a href="#cb2-19" aria-hidden="true" tabindex="-1"></a>      ab <span class="ot">&lt;-</span> mab</span>
<span id="cb2-20"><a href="#cb2-20" aria-hidden="true" tabindex="-1"></a>      a <span class="ot">&lt;-</span> ma</span>
<span id="cb2-21"><a href="#cb2-21" aria-hidden="true" tabindex="-1"></a>      pure&#39; (ab a)</span>
<span id="cb2-22"><a href="#cb2-22" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb2-23"><a href="#cb2-23" aria-hidden="true" tabindex="-1"></a><span class="kw">instance</span> <span class="dt">Applicative</span> (<span class="dt">Cont</span> r) <span class="kw">where</span></span>
<span id="cb2-24"><a href="#cb2-24" aria-hidden="true" tabindex="-1"></a>  <span class="fu">pure</span> a <span class="ot">=</span></span>
<span id="cb2-25"><a href="#cb2-25" aria-hidden="true" tabindex="-1"></a>    <span class="dt">Cont</span> \pure&#39; <span class="ot">-&gt;</span></span>
<span id="cb2-26"><a href="#cb2-26" aria-hidden="true" tabindex="-1"></a>      pure&#39; a</span>
<span id="cb2-27"><a href="#cb2-27" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb2-28"><a href="#cb2-28" aria-hidden="true" tabindex="-1"></a><span class="kw">instance</span> <span class="dt">Bind</span> (<span class="dt">Cont</span> r) <span class="kw">where</span></span>
<span id="cb2-29"><a href="#cb2-29" aria-hidden="true" tabindex="-1"></a>  bind ma amb <span class="ot">=</span></span>
<span id="cb2-30"><a href="#cb2-30" aria-hidden="true" tabindex="-1"></a>    <span class="dt">Cont</span> \pure&#39; <span class="ot">-&gt;</span> <span class="kw">do</span></span>
<span id="cb2-31"><a href="#cb2-31" aria-hidden="true" tabindex="-1"></a>      a <span class="ot">&lt;-</span> ma</span>
<span id="cb2-32"><a href="#cb2-32" aria-hidden="true" tabindex="-1"></a>      b <span class="ot">&lt;-</span> amb a</span>
<span id="cb2-33"><a href="#cb2-33" aria-hidden="true" tabindex="-1"></a>      pure&#39; b</span></code></pre></div>
</multicodeblock-panel>

</multicodeblock-tabs>]]></content>
  </entry>
  <entry>
    <title>React Router and View Transitions</title>
    <link href="https://pete-murphy.github.ioposts/2023-05-17-react-router-view-transitions-api.html"/>
    <id>https://pete-murphy.github.ioposts/2023-05-17-react-router-view-transitions-api.html</id>
    <updated>2023-05-17T00:00:00Z</updated>
    <category term="react"/>
    <category term="react-router"/>
    <category term="view transitions"/>
    <summary></summary>
    <content type="html"><![CDATA[<h2 id="container-transform">Container transform</h2>
<p>Recently I wanted to implement something like what Material UI calls
the <a
href="https://m3.material.io/styles/motion/transitions/transition-patterns#b67cba74-6240-4663-a423-d537b6d21187">container
transform pattern</a>. From the Material 3 docs:</p>
<blockquote>
<p>This pattern is used to seamlessly transform an element to show more
detail, like a card expanding into a details page. […] Persistent
elements are used to seamlessly connect the start and end state of the
transition.</p>
</blockquote>
<p>Here’s a simple example in a GIF</p>
<figure>
<img src="../images/2023-05-21-container-transform.gif"
alt="An example from the Material UI documentation" />
<figcaption aria-hidden="true">An example from the Material UI
documentation</figcaption>
</figure>
<p>The pattern isn’t unique to Material or Google, it’s used for example
in iOS when opening an app from the home screen. I can’t speak on how
this is implemented on mobile, but on the web the implementation has
historically been complicated by the variety of layout rules that need
to be taken into account (Cassie Evans has an excellent <a
href="https://www.youtube.com/watch?v=POBxxUkvHi4">talk about the
difficulties involved here</a>).</p>
<h2 id="flip">FLIP</h2>
<p>The solution that folks seem to have landed on for this is the <a
href="https://aerotwist.com/blog/flip-your-animations/">FLIP
technique</a>. It’s a bit of a magic trick that involves moving the
element to its final state and then applying a <code>transform</code> so
that we can efficiently animate <em>from</em> the initial state.</p>
<blockquote>
<p>… [I]nstead of animating “straight ahead” and potentially doing
expensive calculations on every single frame we precalculate the
animation dynamically and let it play out cheaply.</p>
</blockquote>
<p>There are some libraries for implementing this, like <a
href="https://greensock.com/docs/v3/Plugins/Flip/">GSAP</a> or <a
href="https://github.com/davidkpiano/flipping">Flipping.js</a>. In
React, <code>framer/motion</code> provides a high-level API for using
this technique to achieve “shared layout animations” with its <a
href="https://www.framer.com/motion/layout-group/"><code>LayoutGroup</code></a>
component.</p>
<h2 id="view-transitions-api">View Transitions API</h2>
<p>But there’s also a new high-level API coming to the browser, in the
<a
href="https://developer.mozilla.org/en-US/docs/Web/API/View_Transitions_API">View
Transitions API</a>. This is still a <a
href="https://drafts.csswg.org/css-view-transitions-1/">draft
specification</a> at time of writing, but it’s landed in Chrome. There’s
a nice <a
href="https://developer.chrome.com/docs/web-platform/view-transitions/">write-up
by the Chrome team</a> that serves as a tutorial for using in a
framework-less SPA. Getting this to work in a React app where we don’t
normally manage DOM updates (especially for route transitions) is a bit
trickier.</p>
<!-- <image-loader
placeholder="../images/2023-05-21-demo/small.gif"
full-image="../images/2023-05-21-demo/large.gif"
/> -->
<figure>
<img src="../images/2023-05-21-demo/large.gif"
alt="A simple demo using React Router" />
<figcaption aria-hidden="true">A simple demo using React
Router</figcaption>
</figure>
<p>After a bit of trial-and-error I got a demo working using animated
transitions between routes with React Router. Here’s the repo: <a
href="https://github.com/ptrfrncsmrph/react-view-transitions-api">https://github.com/ptrfrncsmrph/react-view-transitions-api</a>.</p>
<h2 id="some-rough-edges">Some rough edges</h2>
<p><code>startViewTransition</code> takes a callback that
<em>synchronously</em> updates the DOM. The only way I could figure to
do so was using React Router’s <code>useNavigate</code> and a button
with a click handler instead of <code>Link</code> 😔. We then need to
wrap the call to <code>navigate</code> in <code>flushSync</code> to
force the synchronous update.</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode ts"><code class="sourceCode typescript"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true" tabindex="-1"></a><span class="bu">document</span><span class="op">.</span><span class="fu">startViewTransition</span>(() <span class="kw">=&gt;</span> {</span>
<span id="cb1-2"><a href="#cb1-2" aria-hidden="true" tabindex="-1"></a>  ReactDOM<span class="op">.</span><span class="fu">flushSync</span>(() <span class="kw">=&gt;</span> {</span>
<span id="cb1-3"><a href="#cb1-3" aria-hidden="true" tabindex="-1"></a>    <span class="fu">navigate</span>(nextRoute)<span class="op">;</span></span>
<span id="cb1-4"><a href="#cb1-4" aria-hidden="true" tabindex="-1"></a>  })<span class="op">;</span></span>
<span id="cb1-5"><a href="#cb1-5" aria-hidden="true" tabindex="-1"></a>})<span class="op">;</span></span></code></pre></div>
<p>The React docs warn that <code>flushSync</code> should be used as a
“last resort”, and this API does seem to be at odds with the React
mental model that doesn’t normally care about <em>when</em> DOM updates
happen.</p>
<p>Another awkward bit is the need to toggle the
<code>viewTransitionName</code>s for transitioning elements.</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode ts"><code class="sourceCode typescript"><span id="cb2-1"><a href="#cb2-1" aria-hidden="true" tabindex="-1"></a><span class="cf">if</span> (ref<span class="op">.</span><span class="at">current</span>) {</span>
<span id="cb2-2"><a href="#cb2-2" aria-hidden="true" tabindex="-1"></a>  ref<span class="op">.</span><span class="at">current</span><span class="op">.</span><span class="at">style</span><span class="op">.</span><span class="at">viewTransitionName</span> <span class="op">=</span> <span class="st">&quot;movie-image&quot;</span><span class="op">;</span></span>
<span id="cb2-3"><a href="#cb2-3" aria-hidden="true" tabindex="-1"></a>}</span></code></pre></div>
<p>There needs to be exactly one element with the
<code>"movie-image"</code> transition name at any given time, so <a
href="https://developer.chrome.com/docs/web-platform/view-transitions/#transitioning-elements-dont-need-to-be-the-same-dom-element">the
recommendation</a> seems to be to <em>assign the tag name in the event
handler</em>.</p>
<p>A nicer alternative might be to give unique names to each element,
like <code>movie-image-${movie.id}</code> and then select pairs with
<code>::view-transition-group(movie-image-*)</code> but that syntax
doesn’t exist and as far as I can tell the only way of achieving this
currently would require creating just as many rules in the style sheet
as there are pairs of elements you’d want to target. I started <a
href="https://github.com/ptrfrncsmrph/react-view-transitions-api/compare/main...dynamic-style-sheet-rules">going
down that road</a> but couldn’t get it to work (the transitions
<em>did</em> apply but looked janky for reasons I couldn’t understand).
<em>(I think this is also the approach one of the authors of the spec
tried in this repo but seems like it had complications: <a
href="https://github.com/jakearchibald/wordle-analyzer/pull/19">https://github.com/jakearchibald/wordle-analyzer/pull/19</a>)</em></p>
<p>This unique name constraint makes “back navigation” transitions (from
detail to list view) <a
href="https://github.com/ptrfrncsmrph/react-view-transitions-api/commit/9c2a2775a34a2ea8e3a7e1ff90881cb4c8cf4e53#diff-26ad4b834941d9b19ebf9db8082bd202aaf72ea0ddea85f5a8a0cb3c729cc6f2">messy</a>.
The event handler doesn’t have direct access to what we want to target
as the <code>view-transition-new</code> element, so we need to find it
in the DOM, assign the transition name, wait for the transition to
complete, and finally remove the name (so it can be reassigned to the
next item that gets clicked).</p>]]></content>
  </entry>
</feed>
